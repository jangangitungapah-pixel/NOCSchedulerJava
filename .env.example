# NOCScheduler server/runtime environment reference.
#
# Production Cloud Functions obtains Firebase/Google credentials from the
# managed runtime automatically.
#
# If local API code needs direct Firebase Admin SDK access, configure Google
# Application Default Credentials on the developer machine. Never commit
# service-account JSON/private keys into this repository.

FIREBASE_PROJECT_ID=nocschedule1
