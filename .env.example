# NOCScheduler has no server-side runtime in the Spark client-first baseline.
#
# Browser Firebase public configuration is documented in:
# apps/web/.env.example
#
# Do not commit service-account JSON, private keys, or real environment files.
