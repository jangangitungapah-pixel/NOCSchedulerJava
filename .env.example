# WP-F00 intentionally defines no runtime secrets.
# Later phases will add documented public/local variables as features require them.
# Copy this file to an ignored local environment file only when instructed.
# Never commit credentials, private keys, service-account JSON, or production secrets.
