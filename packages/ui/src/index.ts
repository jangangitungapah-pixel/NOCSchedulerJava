/**
 * UI package boundary.
 *
 * Production primitives, patterns, and semantic design tokens are intentionally deferred to WP-F03.
 */
export type UiPackageBoundary = Readonly<{
  packageName: '@nocscheduler/ui';
}>;
